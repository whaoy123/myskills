# System reference modules

Place cross-device rules here: signal-chain closure, rail aggregation, reference loading, fault propagation and other constraints that cannot be decided by checking one component in isolation.
