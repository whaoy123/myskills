# 系统协议｜依赖关系

允许 finish_to_start、start_to_start、not_before、external_wait、all_of、any_of、soft 和 hard。

- hard 未满足时禁止安排，除非用户明确批准。
- soft 未满足时可以安排，但必须提示。
- 任务依赖使用 task_id，不使用易变标题。
- 写入前检查自依赖和依赖环。
- 等待与阻塞统一使用 状态/等待，具体原因和解除条件写正文。

【Planner】
schema: 1
role: config
progress: 0
date_semantics: none
mobility: fixed
privacy: normal
estimate_confidence: high
dependency_mode: all
dependencies:
【/Planner】
