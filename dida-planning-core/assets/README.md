# Assets

`legacy-estimation-seed.json` contains only the old system's included quantitative samples. It is a one-time migration seed, not an ongoing source of truth. After equivalent `[planner-event:v1]` comments are written to Dida, rebuild the local index from Dida and archive/delete this seed.
